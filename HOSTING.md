# ホスティングと費用について

## ホスティング先: Fly.io

- 今のDocker構成（`Dockerfile` / `docker-compose.yml`）をそのまま使える
- 永続ボリュームが標準機能としてあり、`storage/`（アップロード・試聴・書き出しファイル）の保存先にそのまま使える
- 独自ドメインなしでも `https://xxx.fly.dev` のURLとHTTPS証明書が自動で手に入る

検討した他の選択肢:
- **AWS Lightsail / GCP Compute Engine**: 同程度の費用感だが、HTTPS証明書・ドメイン設定を自分たちで組む一手間が増える
- **GCP Compute Engine無料枠(e2-micro)**: 条件次第で$0を狙えるが、メモリが約1GBしかなく音声解析処理でメモリ不足のリスクがある（後述の通り、実際にFly.io上でも1GBでは不足しOOMが発生したため、e2-microでは確実に不足する）。通信量の無料枠も月1GBと少なく、実質「完全無料」ではない
- **Cloudflare Workers/Pages**: PythonバックエンドをそのままではAll動かせないため不採用
- **Cloudflare Tunnel + 自宅PC**: ポート開放・DDNS・HTTPS証明書の手間は解消できるが、PCを常時起動しておく必要があり不採用

本番URL: **https://music-edit-webapp.fly.dev/**（東京(nrt)リージョン）

## 費用構成

### 固定費（毎月ほぼ一定）

| 項目 | 内容 | 月額目安 |
|---|---|---|
| VM | shared-cpu-**2x**（2 vCPU）・**3GB RAM**・常時起動 | $16.40 |
| 永続ボリューム | 5GB | $0.75 |
| 共有IPv4 | 標準で無料 | $0 |
| **固定費 合計** | | **≈$17.15** |

**運用上の教訓（本番投入直後に3件のトラブルが発生・解消済み）**:

1. **メモリ不足によるOOMクラッシュ**: 当初1GB RAMでデプロイしたところ、実際の長さの曲（テストで使っていた短い合成音源とは違う、数分の実データ）をアップロードした際に**メモリ不足でサーバープロセスがOOM Killされる障害が発生した**（ログで`Out of memory: Killed process ... uvicorn`を確認、実測ピークメモリ約1.9GB）。応急対応として、`music_engine/analyze_bpm.py`のビート検出処理を`librosa.load(file_path, sr=None)`（ネイティブサンプルレートのまま解析）から`sr=22050`（固定の解析用サンプルレート）に変更し、ピークメモリを実測で約1.73GB→約483MB（3.7倍削減）した。
2. **↑の副作用でBPM・曲区切りタイミングの精度が大きく低下**: サンプルレートを落としたことでビート検出の時間分解能が半分（約11.6ms→約23.2ms/フレーム）になり、実際の曲でBPM・ブロック境界が明らかにおかしくなる不具合が発生した。**`sr=22050`は revert し、デスクトップ版（`music_edit_app/`、無変更）と同じ`sr=None`（ネイティブレート）に戻した。** メモリ対策は解析側の工夫ではなく、**VMのメモリを3GBに増強する**方向で対応することにした（正確さ最優先。実測ピーク約1.9GBに対し3GBなら十分な余裕がある）。
3. **CPU性能不足による極端な処理遅延**: 実際の長さ（9分台）の曲の解析が`shared-cpu-1x`（1 vCPU共有）では本番で2分以上かかっていた（ローカルでは同じファイルが3秒、同一Dockerイメージでのローカルコンテナ実行でも14秒）。`shared-cpu-2x`（2 vCPU共有）に変更したところ62秒に短縮。料金差はごくわずか（月$1程度）。根本原因（なぜ共有1vCPUだとここまで遅いか）は完全には特定できていないが、実用上は2vCPU化で解消したため深追いはしていない。
4. 上記への保険として、解析処理に**120秒のタイムアウト**を追加した（`songs/routes.DETECT_BEATS_TIMEOUT_SECONDS`）。何らかの理由で処理が異常に長引いた場合も、無限にハングし続けるのではなく504エラーで分かりやすく失敗するようにしている。

いずれも、テスト時は短い合成音源しか使っていなかったため、本番投入して実際の長さの曲をアップロードするまで顕在化しなかった。今後同種の変更をする際は、実際の曲に近い長さ（5〜10分程度）のファイルで検証すること。**正確さ（BPM検出）とコストがトレードオフになる場面では、正確さを優先しコスト側で調整する**という判断を今回は取った。

### 変動費（通信量課金）

理論上は使った分だけ課金されるため青天井になり得るが、以下の安全策で実際の上限を計算可能にしている。

**重要**: Fly.ioの通信量課金はリージョングループごとに異なり、東京(nrt)は**$0.04/GB**（北米・ヨーロッパの$0.02/GBの2倍）。日本からのアクセス速度を優先して東京リージョンを選択したため、以下の試算はすべて$0.04/GBで計算している。

## 乱用防止の仕組み（実費用の上限を作るための安全策）

`backend/app/ratelimit.py` / `backend/app/storage.py` / `backend/app/songs/routes.py` に実装。

| 項目 | 値 | 場所 |
|---|---|---|
| アップロードファイルサイズ上限 | 50MB/ファイル | `storage.MAX_UPLOAD_BYTES` |
| 曲の長さ上限 | 10分/曲 | `songs/routes.MAX_DURATION_SECONDS` |
| サーバー全体のストレージ使用量上限 | 3GB | `storage.STORAGE_CAP_BYTES` |
| アップロード・曲連結のレート制限 | 10回/時間/ユーザー | `ratelimit.upload_limiter` |
| 試聴・書き出し生成のレート制限 | 30回/時間/ユーザー | `ratelimit.combine_limiter` |
| **1日あたりの累計ダウンロード量上限** | **1.8GB/日/ユーザー** | `ratelimit.daily_generation_quota` |

補足:
- 曲の長さ上限は、ファイルサイズ上限だけだと圧縮率の高いフォーマット（低ビットレートMP3等）で長時間の音声をすり抜けさせられてしまうため、実際の再生時間でも別途制限している
- 日次上限は、通信量（＝実際の費用）が発生する**ダウンロード時**にカウントする設計（生成しただけでは課金は発生しないため）。`<audio>`要素のシーク操作はHTTP Rangeリクエストになるため、リクエストされた範囲のバイト数だけを正確にカウントし、水増しされないようにしている
- 日次上限はプロセス再起動（デプロイ・クラッシュ等）をまたいでも維持されるよう、永続ボリューム上の`storage/daily_quota_state.json`に保存している（`time.time()`の壁時計時刻で記録。`time.monotonic()`だと再起動のたびに基準点がリセットされてしまうため使えない）

## 最大費用の試算

**前提**:
- 許可ユーザー数: 最大**3人**（`ALLOWED_EMAILS`、2026年7月時点の実際の設定は1人）
- 通信量課金 **$0.04/GB**（Fly.io・東京(nrt)リージョンの現行料金。将来的な値上げは考慮していない）
- VM（2GB）・ボリューム（5GB）のサイズを手動で変更しない
- Fly.io上のインスタンスは1台のまま（日次上限の実装は単一インスタンス運用が前提。複数台に水平分散する場合は共有ストアへの置き換えが必要）。`fly.toml`で`min_machines_running = 1`・`auto_stop_machines = 'off'`にして常時起動を明示している
- ボリューム作成時に**自動スナップショットが有効**であることを確認済み（`Scheduled snapshots: true`、保持数5）。スナップショット課金は$0.08/GB・最初の10GB無料。`storage/`はTTLで自動掃除され定常的に数百MB〜数GB程度と見込まれるため、通常運用では無料枠に収まる想定だが、正確な課金額は次回の請求で要確認

**計算**:
- 1回の生成・ダウンロードの最大サイズ: 10分×3曲=30分をWAV書き出し ≈ 約300MB
- 1人あたりの日次最大: 1.8GB（日次上限）＋約300MB（上限到達直後の1回分の超過）≈ 2.1GB/日
- 3人が毎日上限まで使い切った場合の月間通信量: 2.1GB × 30日 × 3人 ≈ 189GB
- 変動費: 189GB × $0.04/GB ≈ $7.56/月

**合計: 固定費$17.15 + 変動費$7.56 ≈ $24.71/月が、3人フル稼働時の実質的な上限**（スナップショット課金は含まず）

⚠️ **メモリを2GB→3GBに増強したことで、以前合意した「月額上限$20」を約$5超過している。** BPM精度の不具合修正を優先したための結果であり、この点は改めて相談したい（日次上限をさらに絞る／許可人数を2人に減らす／$25程度まで上限を緩めるなどの選択肢がある）。

現在のユーザー数（1人）であれば、変動費は最大でも 2.1GB×30×1×$0.04 ≈ $2.52/月 程度で、**合計は$19.7/月未満**に収まる。

## ユーザーを増やす場合の再計算方法

```
変動費(月) ≈ (1.8GB + 0.3GB) × 30日 × 人数 × $0.04/GB
合計(月)   ≈ $17.15 + 変動費
```

人数を増やす際は、上記の日次上限（`daily_generation_quota`のmax_bytes）を調整して費用感を再確認すること。3人を超える場合は日次上限をさらに下げる必要がある。

## デプロイ手順（メモ）

```bash
cd music_edit_webapp
flyctl deploy --app music-edit-webapp
```

VMサイズを変更する場合は `fly.toml` の `[[vm]]` セクションを編集してから `flyctl deploy`（`flyctl scale memory` だけだと次回デプロイ時に`fly.toml`の値へ戻されるので注意）。

**既知の問題**: `flyctl deploy`が「Waiting for depot builder...」で数分固まって進まないことが度々ある（Fly.io側のリモートビルダーの問題と見られる）。その場合はCtrl+Cで中断し、もう一度同じコマンドを実行すれば大抵成功する。
